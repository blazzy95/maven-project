pipeline {
    agent any
    stages{
	//tools {
        //maven 'LocalMaven'
    //}
        stage('Build'){
            steps {
                sh 'mvn clean package'
            }
            post {
                success {
                    echo 'Now Archiving...'
                    archiveArtifacts artifacts: '**/target/*.war'
                }
            }
        }
    }
}
